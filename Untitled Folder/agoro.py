import turtle
from turtle import *
class Ball(object):
	def __init__(self,x,y,dx,r,color):
		self.x=x
		self.y=y
		self.dx=dx
		self.r=r
		self.color(color)
		self.shapesize(r/10)
		self.shape("circle")
		self.pu()
		self.(x,y)
def move(self,screen_wedth,screen_height):
	current_x = self.xcor()
	current_y = self.ycor()
	new_x = current_x+self.dx
	new_y = current_y+self.dy
	right_side_ball = new_x+self.r
	left_side_ball = new_x-self.r
	top_side_ball = new_y +self.r
	bottom_side_ball

