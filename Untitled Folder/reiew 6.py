import turtle
from turtle import Turtle
class Square(Turtle):
	def __init__(self,size,color,shape):
		Turtle.__init__(self)
		self.color(color)
		self.shapesize(size)
		self.shape(shape)
		self.shape(circle)
	
square=square

			
